/* c-compiler srclib forwarder (generated by tools/mkgit2srclib.js — ticket #473).
 *
 * libgit2's own build resolved #include "xdiff.h" through
 * -I deps/xdiff. A gucman srclib package has no -I, so the tree
 * resolves it same-dir through this forwarder instead. Do not edit —
 * regenerate with `node tools/mkgit2srclib.js`.
 */
#include "../../deps/xdiff/xdiff.h"
